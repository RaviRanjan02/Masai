// You are given a string, whose size is stored in a variable with the name N

// You have to print all the characters in the string in reverse order, on a new line

// For example, consider the string stored instr = "nrupul", and the value stored in N = 6, then the required output will be

// l
// u
// p
// u
// r
// n
var N=6;
var str="nrupul";
for(var i=N-1;i>=0;i--){
    console.log(str[i]);
}